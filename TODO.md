# TODO: Fix Calls Page - ✅ COMPLETED

**Summary:**
- `src/pages/callspage.jsx` fully rewritten matching `chatpage.jsx` WhatsApp-style UI
  - Topbar, search bar, calls list (avatar/name/time/type icons), clickable items, bottom nav
- Renders 12 calls from `src/data/calls.js` with proper icons: incoming/outgoing/missed
- Click → `/call/${id}` (add route/component later if needed)

**Next:** `npm run dev` → http://localhost:5173/calls to test.
